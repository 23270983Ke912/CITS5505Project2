/*
window.addEventListener("DOMContentLoaded", function () {


    var inputbuttons = document.querySelector('.actions');

    inputbuttons.addEventListener('click', function (event) {
        event.preventDefault();

        return confirm("Are you sure?");
    });

}, false);
*/